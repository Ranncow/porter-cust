/**
 * Plugin constants (hardcoded configuration) for shared code.
 */
export default class Porter_Cust_Shared_Defaults {
    NAME = '@ranncow/porter-base';

    constructor() {
        Object.freeze(this);
    }
}
